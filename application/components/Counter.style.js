/**classe responsavel por estilizar um TextView 
 * utilizando a configuraçãode estilização por arquivo e não por constante.
 */
import {StyleSheet, ColorPropType} from 'react-native'
export default StyleSheet.create({
   
    textCounter:{
            fontSize:20,
            textAlign: 'center',
            margin:50,
            color:'#9C27B0'
    }
});